<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cbas.spartacrafter.satexamples">

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="23"
        android:maxSdkVersion="23" />

    <uses-permission
        android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".TestSelectActivity"
            android:label="@string/app_name">

            <intent-filter>

                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>

        <activity
            android:name=".MathIntroActivity"
            android:label="@string/intro">
        </activity>

        <activity
            android:name=".ReadingIntroActivity"
            android:label="@string/intro">
        </activity>

        <activity
            android:name=".WritingLanguageIntroActivity"
            android:label="@string/intro">
        </activity>

        <activity
            android:name=".EssayIntroActivity"
            android:label="@string/intro">
        </activity>

        <activity
            android:name=".MathTestActivity"
            android:label="@string/math_title">
        </activity>
    </application>
</manifest>
